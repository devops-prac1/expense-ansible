- name: Frontend Setup
  hosts: all
  become: yes
  tasks:
    - name: Install Nginx
      ansible.builtin.dnf:
        name: nginx
        state: latest

    - name: start nginx
      ansible.built.system_service:
        state: restarted
        name: nginx

